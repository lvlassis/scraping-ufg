[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sigaa-cli"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.12.0",
    "python-dotenv>=1.0.0",
    "sigaa-scraper",
]

[project.scripts]
sigaa = "sigaa_cli.main:app"
